# Java 开发规范（RAG 知识库）
## 1. 空指针异常处理规范
1. 调用对象方法前必须做非空判断
2. 优先使用 if (obj != null) 基础判断，业务复杂场景使用 Optional
3. 禁止直接调用 null 对象的任何方法
4. 方法返回集合时禁止返回 null，应返回空集合

## 2. Optional 使用规范
1. Optional.ofNullable() 用于包装可能为 null 的对象
2. orElse(default) 提供默认值，避免空指针
3. 禁止使用 Optional.get() 而不做判断
4. 简单场景优先使用基础 if 判断，保持代码可读性

## 3. 代码编译运行规范
1. public 类必须与文件名一致
2. 类、方法命名遵循驼峰规范
3. 代码缩进统一使用 4 个空格
4. 禁止使用未导入的类，必须显式 import